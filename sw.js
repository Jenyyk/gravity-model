var GHPATH = 'gravity-model';

var APP_PREFIX = 'jnk_';

var VERSION = 'version_00';

var URLS = [
  `${GHPATH}/index.html`,
  `${GHPATH}/styles.css`,
  `${GHPATH}/script.js`,
  `${GHPATH}/plotly.js`,
  `${GHPATH}/bodyscript.js`,
  `${GHPATH}/pkg/gravity_model.js`,
  `${GHPATH}/pkg/gravity_model_bg.wasm`,
];
